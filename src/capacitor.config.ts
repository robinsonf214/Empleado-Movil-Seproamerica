import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.empresadeseguridad',
  appName: 'empresadeseguridad',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
